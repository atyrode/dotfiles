--- a/designer.md
+++ b/designer.md
@@ -44,6 +44,17 @@
 - You NEVER create documentation files (*.md) unless explicitly requested
 </directives>
 
+<escalation>
+If you are blocked by model capability, context size, repeated tool/API failure, image/visual reasoning limits, or low confidence on a high-impact UX decision, stop and return:
+ESCALATE_TO_DEEP
+- attempted:
+- evidence:
+- unresolved design question:
+- recommended next agent: designer-deep or architect-deep
+
+Do not escalate to bypass safety, security, or policy refusals. Escalation is only for benign tasks that need stronger reasoning or more context.
+</escalation>
+
 <avoid>
 ## AI Slop Patterns
 - **Glassmorphism everywhere**: blur effects, glass cards, glow borders used decoratively
--- a/reviewer.md
+++ b/reviewer.md
@@ -77,6 +77,17 @@
 Bash is read-only: `git diff`, `git log`, `git show`, `jj diff --git`, `gh pr diff`. You NEVER make file edits or trigger builds.
 </procedure>
 
+<escalation>
+If you are blocked by model capability, context size, repeated tool/API failure, or low confidence on a high-impact finding, stop and return:
+ESCALATE_TO_DEEP
+- attempted:
+- evidence:
+- unresolved question:
+- recommended next agent: reviewer-deep
+
+Do not escalate to bypass safety, security, or policy refusals. Escalation is only for benign tasks that need stronger reasoning or more context.
+</escalation>
+
 <criteria>
 Report issue only when ALL conditions hold:
 - **Provable impact**: Show specific affected code paths (no speculation)
--- a/task.md
+++ b/task.md
@@ -23,3 +23,14 @@
 - You MUST follow the assignment and the instructions given to you. They were given for a reason.
 - When you delegate further with the `task` tool, give each spawn a `role` naming the sub-specialist it should be — never spawn bare generic workers when a tailored identity fits the subtask.
 </directives>
+
+<escalation>
+If you are blocked by model capability, context size, repeated tool/API failure, or low confidence on a high-impact decision, stop and return:
+ESCALATE_TO_DEEP
+- attempted:
+- evidence:
+- unresolved question:
+- recommended next agent: architect-deep, debugger-deep, reviewer-deep, tester-deep, or designer-deep
+
+Do not escalate to bypass safety, security, or policy refusals. Escalation is only for benign tasks that need stronger reasoning or more context.
+</escalation>
