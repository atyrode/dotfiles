tools:
  approvalMode: yolo
  approval:
    bash: allow
    eval: allow
    browser: allow
    task: allow
    github: allow

secrets:
  enabled: true

isolation:
  backend: auto

task:
  isolation:
    enabled: true
    merge: patch
    commits: generic
