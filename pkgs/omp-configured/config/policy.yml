tools:
  approvalMode: yolo
  approval:
    bash: allow
    eval: allow
    browser: allow
    task: allow
    github: allow

secrets:
  enabled: true

task:
  isolation:
    mode: auto
    merge: patch
    commits: generic
